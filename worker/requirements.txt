# Procesamiento asíncrono y mensajería
aio_pika>=9.0.0         # cliente AMQP asincrónico compatible con asyncio y RabbitMQ

# Cliente para la base vectorial
qdrant-client>=1.5.0    # cliente oficial de Qdrant

# Embeddings (con soporte GPU y modelos HF)
sentence-transformers>=2.2.2
torch>=2.7.1            # ya incluido en la imagen base, puedes omitirlo si lo prefieres

# Preprocesamiento y extracción de contenido
unstructured[all-docs]>=0.12.2  # soporte completo para PDF, DOCX, HTML, etc.
pdfminer.six>=20221105         # extrae texto de PDFs (parte de unstructured pero puede requerirse explícitamente)
python-docx>=0.8.11            # extrae texto de .docx
